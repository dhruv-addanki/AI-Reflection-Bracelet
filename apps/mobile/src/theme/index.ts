import { colors, radii, spacing, typography } from "@bracelet/ui-tokens";

export const theme = {
  colors,
  spacing,
  radii,
  typography,
  typefaces: {
    heading: "Georgia",
    body: "System"
  }
} as const;
